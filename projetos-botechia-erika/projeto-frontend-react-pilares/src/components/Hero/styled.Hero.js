import styled from "styled-components";

export const HeroContainer = styled.section`
    padding-top: 20px;
    height: 50vh;
    background-color: var(--gray-10);
    text-align: center;
    display: flex;
    align-items: center;
    justify-content: space-around;
    font-size: 2rem;
    color: var(--greenyellow-default);


    span{
        color: var(--white-default);
       // font-size: 1.6rem;
    }
`